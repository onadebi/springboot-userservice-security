spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_security
    username: onadebi
    password: onadebi
    driver-class-name: org.postgresql.Driver
  security:
    user:
      name: user
      password: pwd

  jpa:
    hibernate:
      ddl-auto: create-drop
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  jwtSecret: supersignaturekey